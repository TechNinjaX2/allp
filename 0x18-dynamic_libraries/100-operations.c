int add(int a, int b)
{
	int c;
	return(c = a + b);
}
int sub(int a, int b)
{
        int c;
        return(c = a - b);
}
int mul(int a, int b)
{
        int c;
        return(c = a * b);
}
int div(int a, int b)
{
        int c;
        return(c = a / b);
}
int mod(int a, int b)
{
	int c;
	return(c = a % b);
}
